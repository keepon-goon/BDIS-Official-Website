# 跳转功能修改文档

## 任务概述

为首页轮播图和新闻列表添加条件跳转功能，使得招新相关的内容能够直接跳转到"加入我们"页面的报名表模块，其他内容跳转到"正在建设中"页面。

## 需求说明

### 1. 首页轮播图（Hero Banner）
- **需求**：只有招新相关的轮播图需要添加跳转链接
- **跳转规则**：
  - 招新图片 → 跳转到 `/info/join#registration`（加入我们页面的报名表模块）
  - 其他图片 → 跳转到 `/wip`（正在建设中页面）

### 2. 新闻列表模块
- **需求**：招新通告类型的新闻需要添加跳转链接
- **跳转规则**：
  - 招新通告（category: `recruit`） → 跳转到 `/info/join#registration`
  - 其他新闻 → 跳转到 `/wip`

## 修改文件列表

1. `src/components/HomeBanner.vue` - 首页轮播图组件
2. `src/components/HomeNews.vue` - 首页新闻列表组件
3. `src/components/info/Join.vue` - 加入我们页面组件
4. `src/router/index.js` - 路由配置文件

---

## 详细修改内容

### 1. HomeBanner.vue（首页轮播图组件）

#### 修改位置：模板部分
**修改前：**
```vue
<el-carousel-item v-for="item in activeItems" :key="item.id">
  <div class="banner-slide" :style="slideStyle(item.imageUrl)"></div>
</el-carousel-item>
```

**修改后：**
```vue
<el-carousel-item v-for="item in activeItems" :key="item.id">
  <div 
    class="banner-slide" 
    :style="slideStyle(item.imageUrl)"
    @click="handleBannerClick(item)"
    :class="{ 'banner-clickable': isRecruitBanner(item) }"
  ></div>
</el-carousel-item>
```

**说明：**
- 添加 `@click` 事件处理器
- 添加条件样式类 `banner-clickable`（仅招新图片可点击）

#### 修改位置：脚本部分
**新增代码：**
```javascript
import { useRouter } from 'vue-router'

const router = useRouter()

// 判断是否为招新图片
const isRecruitBanner = (item) => {
  return item.title && item.title.includes('招新')
}

// 处理轮播图点击
const handleBannerClick = (item) => {
  if (isRecruitBanner(item)) {
    // 招新图片：跳转到加入我们页面的报名表模块
    router.push('/info/join#registration')
  } else {
    // 其他图片：跳转到正在建设中页面
    router.push('/wip')
  }
}
```

**说明：**
- 导入 `useRouter` 用于路由跳转
- `isRecruitBanner()` 函数：通过检查 `title` 字段是否包含"招新"来判断是否为招新图片
- `handleBannerClick()` 函数：根据是否为招新图片执行不同的跳转逻辑

#### 修改位置：样式部分
**新增代码：**
```css
.banner-clickable {
  cursor: pointer;
}
```

**说明：**
- 为可点击的轮播图添加鼠标手型样式，提升用户体验

---

### 2. HomeNews.vue（首页新闻列表组件）

#### 修改位置：脚本部分
**修改前：**
```javascript
const goDetail = (id) => router.push('/wip')//V2版本再做细节页面
```

**修改后：**
```javascript
const goDetail = (id) => {
  // 找到对应的新闻项
  const newsItem = newsList.value.find(item => item.id === id)
  // 如果是招新通告，跳转到加入我们页面的报名表模块
  if (newsItem && newsItem.category === 'recruit') {
    router.push('/info/join#registration')
  } else {
    // 其他新闻：跳转到正在建设中页面
    router.push('/wip')
  }
}
```

**说明：**
- 通过 `id` 查找对应的新闻项
- 判断 `category` 字段是否为 `'recruit'`（招新通告）
- 根据分类执行不同的跳转逻辑

---

### 3. Join.vue（加入我们页面组件）

#### 修改位置：模板部分
**修改前：**
```vue
<div class="cta-card">
```

**修改后：**
```vue
<div class="cta-card" id="registration">
```

**说明：**
- 为报名表模块容器添加 `id="registration"` 锚点标识
- 用于路由跳转时定位到该模块

---

### 4. router/index.js（路由配置文件）

#### 修改位置：路由实例配置
**修改前：**
```javascript
const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes,
})
```

**修改后：**
```javascript
const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes,
  scrollBehavior(to, from, savedPosition) {
    // 如果有锚点，滚动到锚点位置
    if (to.hash) {
      return {
        el: to.hash,
        behavior: 'smooth',
        top: 100 // 距离顶部100px，避免被导航栏遮挡
      }
    }
    // 如果有保存的位置（浏览器前进/后退），使用保存的位置
    if (savedPosition) {
      return savedPosition
    }
    // 否则滚动到顶部
    return { top: 0 }
  }
})
```

**说明：**
- 添加 `scrollBehavior` 配置，实现锚点跳转时的平滑滚动
- 当路由包含锚点（`#registration`）时，自动滚动到对应元素
- `top: 100` 确保内容不被顶部导航栏遮挡
- `behavior: 'smooth'` 实现平滑滚动效果

---

## 功能说明

### 跳转逻辑流程图

```
首页轮播图点击
    ↓
判断 title 是否包含"招新"
    ├─ 是 → /info/join#registration（加入我们页面 - 报名表模块）
    └─ 否 → /wip（正在建设中页面）

新闻列表项点击
    ↓
判断 category 是否为 'recruit'
    ├─ 是 → /info/join#registration（加入我们页面 - 报名表模块）
    └─ 否 → /wip（正在建设中页面）
```

### 判断条件

1. **轮播图判断**：
   - 检查 `item.title` 是否包含字符串"招新"
   - 示例：`"2025-2026年度工作室招新启动"` → 包含"招新" → 跳转到报名表

2. **新闻判断**：
   - 检查 `newsItem.category` 是否等于 `'recruit'`
   - 示例：`category: "recruit"` → 跳转到报名表

---

## 测试方法

### 1. 测试轮播图跳转

**测试步骤：**
1. 启动前端项目：`npm run dev:mock`
2. 访问首页：`http://localhost:5173`
3. 找到标题包含"招新"的轮播图（如："2025-2026年度工作室招新启动"）
4. 点击该轮播图
5. **预期结果**：页面跳转到 `/info/join`，并自动滚动到报名表模块（"获取报名表"按钮所在位置）

**测试其他轮播图：**
1. 点击其他不包含"招新"的轮播图
2. **预期结果**：跳转到 `/wip`（正在建设中页面）

### 2. 测试新闻列表跳转

**测试步骤：**
1. 在首页找到分类为"招新通告"的新闻项
2. 点击该新闻项
3. **预期结果**：页面跳转到 `/info/join`，并自动滚动到报名表模块

**测试其他新闻：**
1. 点击分类为"活动组会"、"综合新闻"等其他类型的新闻
2. **预期结果**：跳转到 `/wip`（正在建设中页面）

### 3. 测试锚点滚动

**测试步骤：**
1. 直接访问：`http://localhost:5173/info/join#registration`
2. **预期结果**：页面加载后自动滚动到报名表模块，且位置合适（不被导航栏遮挡）

---

## 技术要点

### 1. 路由锚点跳转
- Vue Router 支持在路径中使用锚点（`#`）
- 通过 `scrollBehavior` 配置实现平滑滚动

### 2. 条件判断
- 轮播图：使用字符串包含判断（`includes()`）
- 新闻：使用精确匹配判断（`===`）

### 3. 用户体验优化
- 添加 `cursor: pointer` 样式，提示用户可点击
- 使用平滑滚动（`behavior: 'smooth'`）
- 设置合适的滚动偏移量（`top: 100`），避免内容被导航栏遮挡

---

## 注意事项

1. **数据格式要求**：
   - 轮播图数据中的 `title` 字段必须包含"招新"才能被识别为招新图片
   - 新闻数据中的 `category` 字段必须为 `'recruit'` 才能被识别为招新通告

2. **锚点 ID**：
   - 报名表模块的容器必须保持 `id="registration"`，否则锚点跳转会失败

3. **路由配置**：
   - `scrollBehavior` 中的 `top: 100` 可以根据实际导航栏高度调整

4. **兼容性**：
   - 平滑滚动（`behavior: 'smooth'`）在现代浏览器中支持良好
   - 如需兼容旧浏览器，可以考虑使用 polyfill 或降级方案

---

## 修改日期

2026年3月14日



---

## 附录：相关文件路径

- `src/components/HomeBanner.vue`
- `src/components/HomeNews.vue`
- `src/components/info/Join.vue`
- `src/router/index.js`

