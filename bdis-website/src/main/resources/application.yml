server:
  port: 8081  # 解决端口占用问题

# # spring:
#   datasource:
#     driver-class-name: com.mysql.cj.jdbc.Driver
#     # 改这里：把 你的数据库名 换成宝塔里你创建的数据库名字
#     # url: jdbc:mysql://localhost:3306/bdis_work?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
#     # username: bdis_work
#     # password: ${BDIS_DB_PASSWORD:}
#     url: jdbc:mysql://127.0.0.1:3306/bdis_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#     username: root
#     password: ${BDIS_DB_PASSWORD:}

spring:
  datasource:
    url: ${BDIS_DB_URL:jdbc:mysql://localhost:3306/bdis?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${BDIS_DB_USERNAME:root}
    password: ${BDIS_DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: ${BDIS_MAIL_HOST:smtp.163.com}
    port: ${BDIS_MAIL_PORT:465}
    username: ${BDIS_MAIL_USERNAME:}
    password: ${BDIS_MAIL_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      mail.smtp.from: ${BDIS_MAIL_USERNAME:}

mail:
  recipient: ${BDIS_MAIL_RECIPIENT:${BDIS_MAIL_USERNAME:}}

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bdis.bdis_website.entity  # 确保包路径与你的项目一致
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: ${BDIS_JWT_SECRET:change-me-before-deploying}
  expiration: ${BDIS_JWT_EXPIRATION:86400000}

upload:
  path: ${user.dir}/uploads
